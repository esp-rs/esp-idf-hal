//! The examples of the `esp-idf-sys`, `esp-idf-hal` and `esp-idf-svc` crates.
//!
//! This crate has no library code of its own: the examples live in the `examples` directory
//! next to this file and are built from the root of the repository, e.g.
//! `cargo build --example wifi`.
