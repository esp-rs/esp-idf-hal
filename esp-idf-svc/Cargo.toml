[package]
name = "esp-idf-svc"
version = "0.23.1"
authors = ["Ivan Markov <ivan.markov@gmail.com>"]
edition = "2018"
categories = ["embedded", "hardware-support"]
keywords = ["embedded", "svc", "idf", "esp-idf", "esp32"]
description = "Implementation of the embedded-svc traits for ESP-IDF (Espressif's IoT Development Framework)"
repository = "https://github.com/ivmarkov/esp-idf-svc"
license = "MIT OR Apache-2.0"
readme = "README.md"

[features]
default = ["std"]

std = ["alloc", "anyhow/std", "esp-idf-sys/std", "embedded-svc/std"]
alloc = ["cstr_core/alloc", "anyhow"]

binstart = []
libstart = []
experimental = ["embedded-svc/experimental"]

[dependencies]
enumset = "1"
log = "0.4"
cstr_core = "0.2"
anyhow = { version = "1.0", features = ["backtrace"], optional = true }
mutex-trait = "0.2"
embedded-svc = "0.10.3"
esp-idf-sys = "0.20.8"

[build-dependencies]
embuild = "0.24"
anyhow = "1"
